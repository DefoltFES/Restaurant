<Page x:Class="RestaurantApp.Views.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RestaurantApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Frame x:Name="RegisterFrame"  Grid.Column="0" Background="Transparent" HorizontalAlignment="Stretch"></Frame>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="1" Background="Transparent">
            <Button Click="GoBack" Background="Transparent" BorderBrush="Transparent">
            <Path Data="{StaticResource BackRegisterButton}" Fill="{StaticResource SecondColor}"></Path>
            </Button>
            <TextBlock Margin="58,0,164,0" Foreground="{StaticResource SecondColor}" FontFamily="{StaticResource Medium}" FontSize="50" Text="Регистрация"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
